# serverCfg.yml
server:
  port: 13042

fakeDataGenerator:
# --chunk 3525 --visits 30 --objects 10000 example_spec.py
  arguments: '--visits 30 --objects 1000'
  cfgFileName: fakeGenSpec.py

partitioner:
  cfgDir: fakePartitionerCfgs

